#include "main.h"
/**
 * _abs - function
 *
 * @n: int
 *
 * Description: 'n' is the parameter int
 *
 * Return:the absolute of a number
 */
int _abs(int n)
{
	if (n >= 0)
	{
	return (n);
	}
	else
	{
	return (-n);
	}
}
